/**************************************************************************************
 *
 * DO NOT EDIT THIS FILE MANUALLY
 * THIS FILE IS GENERATED BY
 * modules\S32\Tools\MdibBuilders\RunGenerator.cmd
 *
 **************************************************************************************/

#pragma once

#include <Framework/Mdib/Public/Model/Component/BatteryState.h>

namespace DFL
{
namespace Mdib
{
namespace Impl
{
/**
 * @brief Calls the visitor for each member.
 *
 * @param vis visitor
 * @param batteryState @ref DFL::Mdib::BatteryState value to visit
 * @ingroup Mdib
 */
template <class Visitor>
void visitMembers(Visitor& vis, const BatteryState& batteryState)
{
    vis(batteryState.activationState());
    vis(batteryState.operatingHours());
    vis(batteryState.operatingCycles());
    vis(batteryState.calibrationInfo());
    vis(batteryState.nextCalibrationInfo());
    vis(batteryState.physicalConnector());
    vis(batteryState.chargeStatus());
    vis(batteryState.chargeCycles());
    vis(batteryState.capacityRemaining());
    vis(batteryState.voltage());
    vis(batteryState.current());
    vis(batteryState.temperature());
    vis(batteryState.remainingBatteryTime());
    vis(batteryState.extensions());
}
}
}
}
